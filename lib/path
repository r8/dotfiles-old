# Custom bin folders
export PATH=$PATH:$HOME/.composer/vendor/bin

# Create custom bin folders
mkdir -p $HOME/.composer/vendor/bin

if [[ -f $HOME/.path ]]; then
  # Load local paths
  source $HOME/.path
fi
